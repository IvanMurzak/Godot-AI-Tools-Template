# Godot + .NET regenerate these; the addon is installed by CI (godot-cli install-plugin).
.godot/
[Bb]in/
[Oo]bj/
addons/
*.uid
# install-plugin drops MCP skill stubs here; the live run-tool E2E creates scenes via scene-create.
.claude/
*.tscn
